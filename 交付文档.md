# Mycardgame 项目交付文档

---

## 一、项目简介

Mycardgame 是一个基于 Cocos2d-x 引擎开发的卡牌游戏项目，支持 Windows 平台。项目采用 CMake 构建，包含完整的游戏逻辑、资源管理、关卡配置、存档功能等模块，结构清晰，易于扩展和维护。

## 二、项目结构说明

```
├── CMakeLists.txt                # CMake 构建脚本
├── Mycardgame.sln                # Visual Studio 解决方案
├── Classes/                      # 核心游戏代码
│   ├── AppDelegate.*             # 应用入口及生命周期管理
│   ├── GameScene.*               # 主游戏场景逻辑
│   ├── controllers/              # 游戏控制器
│   ├── managers/                 # 管理器类
│   ├── models/                   # 数据模型
│   ├── services/                 # 服务类
│   ├── utils/                    # 工具类
│   ├── views/                    # 视图类
│   └── configs/                  # 配置相关代码
├── cocos2d/                      # 引擎源码
├── extensions/                   # 引擎扩展
├── external/                     # 第三方库
├── Resources/                    # 游戏资源
│   ├── level.json                # 关卡配置
│   ├── fonts/                    # 字体资源
│   └── res/                      # 其他资源
├── build/                        # 编译输出
├── proj.win32/                   # Windows 平台工程
├── show_test/                    # 演示与测试
└── README.md                     # 项目说明文档
```

## 三、主要功能模块

### 1. 游戏主流程
- 游戏启动、场景切换、主循环由 `AppDelegate` 和 `GameScene` 管理。

### 2. 卡牌模型与视图
- `models/CardModel.*` 定义卡牌数据结构。
- `views/CardView.*` 负责卡牌渲染与交互。

### 3. 关卡配置与加载
- `configs/loaders/LevelLoader.*` 解析 `Resources/level.json`，加载关卡数据。
- `configs/models/LevelConfig.h` 定义关卡配置结构。

### 4. 游戏控制器
- `controllers/GameController.*` 管理游戏逻辑。
- `controllers/GameController_Save.cpp` 实现存档相关逻辑。

### 5. 撤销管理
- `managers/UndoManager.*` 支持操作撤销与重做。

### 6. 资源管理
- 资源统一管理与加载，资源文件位于 `Resources/`。

### 7. 工具与服务
- `utils/JsonUtils.*` 提供 JSON 解析工具。
- `services/SaveGameService.*` 提供存档服务。

## 四、编译与运行

### 1. 环境要求
- Windows 10/11
- CMake 3.10+
- Visual Studio 2019/2022
- Cocos2d-x 4.x

### 2. 编译步骤
1. 使用 CMake 配置项目，生成 Visual Studio 工程：
   ```powershell
   cmake -S . -B build
   ```
2. 打开 `build/Mycardgame.sln`，使用 Visual Studio 编译解决方案。
3. 运行生成的可执行文件（如 `show_test/Mycardgame.exe`）。

### 3. 资源说明
- 所有游戏资源位于 `Resources/` 目录，关卡配置为 `level.json`。

## 五、代码模块详细说明

### 1. 应用入口与主场景

- **AppDelegate.cpp/h**
  - 负责 Cocos2d-x 引擎初始化、OpenGL 上下文设置、资源加载、场景切换。
  - 生命周期管理（进入后台/前台）。
  - 启动后加载 `GameScene`。

- **GameScene.cpp/h**
  - 游戏主场景，负责界面布局（主牌区、堆牌区）。
  - 创建并管理 `GameController`，负责游戏流程。
  - 提供撤销按钮，调用控制器的撤销逻辑。

### 2. 游戏控制器与核心逻辑

- **controllers/GameController.cpp/h**
  - 游戏主逻辑控制器，负责关卡加载、卡牌布局、用户交互、游戏状态管理。
  - 通过 `LevelLoader` 加载关卡配置，生成卡牌视图。
  - 处理卡牌点击事件，判断有效操作，执行配对、移动等逻辑。
  - 协调模型（CardModel）与视图（CardView）的状态同步。
  - 管理撤销操作（UndoManager），支持撤销/重做。
  - 提供存档接口，调用 `SaveGameService` 保存当前状态。

- **controllers/GameController_Save.cpp**
  - 实现游戏存档相关逻辑，辅助主控制器进行状态保存。

### 3. 数据模型与视图

- **models/CardModel.cpp/h**
  - 卡牌数据结构，包含花色、点数、唯一ID等属性。
  - 支持 JSON 序列化/反序列化，便于存档与关卡配置。
  - 枚举类型定义卡牌花色和点数，便于扩展。

- **views/CardView.cpp/h**
  - 卡牌视图，继承自 Sprite，负责渲染卡牌图像、花色、点数。
  - 持有 CardModel 指针，展示对应数据。
  - 支持自定义布局（如角标、花色图标），便于美术扩展。

### 4. 撤销与命令管理

- **managers/UndoManager.cpp/h**
  - 撤销管理器，采用命令模式，维护可撤销操作栈。
  - 每个操作封装为 UndoCommand，支持 lambda 回调。
  - 提供 addAction/undo/clear 等接口，供控制器调用。

### 5. 存档与读档服务

- **services/SaveGameService.cpp/h**
  - 单例服务类，负责游戏状态的保存与读取。
  - 存档内容包括关卡ID、卡牌状态、分数、撤销步数等。
  - 使用 RapidJSON 序列化，存储于本地 `savegame.json` 文件。
  - 提供 saveGame/loadGame 等接口，供控制器调用。

### 6. 工具类

- **utils/JsonUtils.cpp/h**
  - JSON 工具类，封装 RapidJSON 的解析与文件读取。
  - 提供 parseJsonString/loadJsonFromFile 等静态方法，便于各模块调用。

### 7. 关卡配置与加载

- **configs/loaders/LevelLoader.cpp/h**
  - 关卡加载器，负责解析关卡 JSON 文件，生成 LevelConfig 数据结构。
  - 支持多关卡扩展，便于关卡设计师配置。
  - 解析卡牌初始布局、堆牌区等信息。

- **configs/models/LevelConfig.h**
  - 关卡配置结构体，包含主牌区和堆牌区的卡牌初始信息。
  - 支持扩展更多关卡属性（如特殊规则、目标分数等）。

### 资源目录
- `Resources/level.json`：关卡配置文件，采用 JSON 格式。
- `Resources/fonts/`：字体资源。
- `Resources/res/`：图片、音效等其他资源。

## 六、数据流与交互说明

1. **启动流程**  
   AppDelegate 初始化后进入 GameScene，GameScene 创建 GameController 并加载关卡。

2. **关卡加载**  
   GameController 调用 LevelLoader 解析关卡 JSON，生成卡牌配置，创建 CardModel 和 CardView。

3. **用户交互**  
   用户点击卡牌，GameController 响应事件，判断操作合法性，更新模型和视图。

4. **撤销操作**  
   每次有效操作由 UndoManager 记录，用户点击撤销按钮时，UndoManager 执行最近一次撤销命令。

5. **存档与读档**  
   游戏状态变化时，GameController 可调用 SaveGameService 保存当前状态，支持断点续玩。

---

## 七、扩展建议

- 增加更多关卡与卡牌类型
- 优化 UI 与动画效果

## 八、项目可扩展性说明

本项目采用模块化设计，核心逻辑、数据模型、资源配置和视图层均支持灵活扩展。以下通过“添加 Joker 牌”举例说明：

### 1. 枚举与数据模型扩展

- 所有卡牌类型通过枚举 `CardFaceType` 管理，新增牌型只需在枚举中添加成员，如：
  ```cpp
  enum class CardFaceType {
      // ...已有类型...
      CFT_JOKER, // 新增 Joker 牌
      // ...existing code...
  };
  ```
- 若 Joker 牌有特殊花色，可在 `CardSuitType` 增加 `CST_JOKER`。

### 2. 关卡配置与解析扩展

- 关卡 JSON 文件可直接配置新牌型：
  ```json
  {
    "CardFace": 14, // 例如 14 代表 Joker
    "CardSuit": -1,
    "Position": { "x": 100, "y": 200 }
  }
  ```
- `LevelLoader` 解析时自动识别新类型，无需重构。

### 3. 视图层扩展

- `CardView` 渲染时根据 `CardFaceType` 判断是否为 Joker，加载专属图片或标识：
  ```cpp
  if (_model->getFace() == CardFaceType::CFT_JOKER) {
      // 加载 Joker 图片
  }
  ```

### 4. 游戏逻辑扩展

- `GameController` 可在配对、得分等逻辑中增加 Joker 牌特殊规则，代码分支清晰，易于维护。
  - 例如：
    - 在卡牌配对判断时，增加 Joker 牌的万能配对逻辑：
      ```cpp
      if (cardA->getFace() == CardFaceType::CFT_JOKER || cardB->getFace() == CardFaceType::CFT_JOKER) {
          // Joker 可与任意牌配对
          return true;
      }
      // ...原有配对逻辑...
      ```
    - 在得分计算时，Joker 牌可赋予特殊分数或效果：
      ```cpp
      if (card->getFace() == CardFaceType::CFT_JOKER) {
          score += jokerBonus;
      }
      ```
    - 在关卡胜利判定、特殊事件等逻辑中，灵活扩展 Joker 牌的作用。
  - 由于核心逻辑采用分层和枚举判断，添加新牌型仅需在相关分支补充规则，无需大规模重构。
  - 通过 UndoManager，Joker 相关操作也可被撤销，保证一致性。

- 这种设计使得后续扩展如“道具牌”、“特殊事件牌”等也能以类似方式快速集成。

